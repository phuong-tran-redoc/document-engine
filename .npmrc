# Registry configuration for @phuong-tran-redoc scope packages (GitHub Packages)
@phuong-tran-redoc:registry=https://npm.pkg.github.com

# Default registry (npmjs.org for other packages)
registry=https://registry.npmjs.org/

# GitHub Packages authentication token
//npm.pkg.github.com/:_authToken=${NPM_PUBLISH_TOKEN}

# Optional: Enable strict SSL
strict-ssl=true

# Optional: Save exact versions (recommended for libraries)
save-exact=false

# Optional: Package lock file
package-lock=true
