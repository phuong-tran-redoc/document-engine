# document-engine-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build document-engine-core` to build the library.

## Running unit tests

Run `nx test document-engine-core` to execute the unit tests via [Jest](https://jestjs.io).
