export interface SidebarConfig {
  width: {
    sideFull: string;
    sideFullPx: number;
    sideCompact: string;
    sideCompactPx: number;
    overFull: string;
    overFullPx: number;
    overCompact: string;
    overCompactPx: number;
  };
}
