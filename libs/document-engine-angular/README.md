# document-engine-angular

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build document-engine-angular` to build the library.

## Running unit tests

Run `nx test document-engine-angular` to execute the unit tests via [Jest](https://jestjs.io).
